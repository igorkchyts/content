documentation_complete: true

prodtype: ocp4

title: 'Ensure that kubeadmin is removed'

description: |-
  Ensure that kubeadmin is removed

rationale: |-
  Ensure that kubeadmin is removed

severity: medium

template:
  name: yamlfile_value
  vars:
    ocp_data: "true"
    filepath: '/api/v1/namespaces/default/secrets/kubeadmin'
    yamlpath: '.data.kubeadmin'
    values:
      - value: '.*'
        type: "string"
